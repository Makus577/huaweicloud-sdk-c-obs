# 断点续传HTTPS安全特性测试设计

## 测试目标

本文档详细描述了对断点续传功能中HTTPS安全特性进行测试的设计方案，包括测试场景、测试步骤和预期结果。

## 测试场景概览

| 场景编号 | 场景名称 | 安全特性 |
|---------|---------|---------|
| 场景一 | 标准TLS双向认证 | 国际算法，双向认证 |
| 场景二 | 国密单向认证 | 国密算法，单向认证 |
| 场景三 | 国密双向认证 | 国密算法，双向认证，双证书 |
| 场景四 | 国密密码套件配置 | 国密套件自定义 |

## 公共测试参数

- **测试文件大小**: 50MB
- **并发线程数**: 3
- **分段大小**: 5MB
- **验证方式**: MD5校验

## 场景一：标准TLS双向认证

### 测试目的
验证断点续传功能在标准TLS双向认证场景下的正确性。

### 前置条件
1. OBS服务端已配置标准TLS双向认证
2. 有效的服务端CA证书
3. 有效的客户端证书和私钥

### SSL配置
```c
// 基础SSL配置
server_cert_path = "/path/to/ca.pem";

// 双向认证配置
mutual_ssl_switch = OBS_MUTUAL_SSL_OPEN;
client_cert_path = "/path/to/client_cert.pem";
client_key_path = "/path/to/client_key.pem";
// 可选：client_key_password = "password";
```

### 测试步骤

#### 上传测试
1. 准备50MB测试文件，计算MD5值
2. 初始化obs_options结构体
3. 配置SSL参数（标准TLS双向认证）
4. 初始化分段上传任务
5. 模拟中断（上传2个分段后中断）
6. 记录已上传分段信息
7. 重新初始化断点续传任务
8. 继续上传剩余分段
9. 完成分段上传，合并对象
10. 验证对象MD5值与源文件一致

#### 下载测试
1. 获取已上传对象的元数据
2. 初始化obs_options结构体
3. 配置SSL参数（标准TLS双向认证）
4. 初始化分段下载任务（3个并发，5MB分段）
5. 模拟中断（下载2个分段后中断）
6. 记录已下载分段信息
7. 重新初始化断点续传任务
8. 继续下载剩余分段
9. 合并分段文件
10. 验证文件MD5值与源对象一致

### 预期结果
- 上传任务成功完成，数据完整性校验通过
- 下载任务成功完成，数据完整性校验通过
- 断点续传功能正常工作，中断后能够正确恢复

---

## 场景二：国密单向认证

### 测试目的
验证断点续传功能在国密单向认证场景下的正确性。

### 前置条件
1. OBS服务端已配置国密SSL
2. 有效的国密CA证书
3. 服务端支持国密算法（SM2/SM3/SM4）

### SSL配置
```c
// 启用国密模式
gm_mode_switch = OBS_GM_MODE_OPEN;

// 国密CA证书
server_cert_path = "/path/to/sm2_ca.pem";

// 国密密码套件（可选，有默认值）
ssl_cipher_list = "ECDHE-SM2-WITH-SM4-SM3:ECDHE-SM2-WITH-SM4-GCM-SM3";
```

### 测试步骤

#### 上传测试
1. 准备50MB测试文件，计算MD5值
2. 初始化obs_options结构体
3. 配置SSL参数（国密单向认证）
4. 初始化分段上传任务
5. 模拟中断（上传2个分段后中断）
6. 记录已上传分段信息
7. 重新初始化断点续传任务
8. 继续上传剩余分段
9. 完成分段上传，合并对象
10. 验证对象MD5值与源文件一致

#### 下载测试
1. 获取已上传对象的元数据
2. 初始化obs_options结构体
3. 配置SSL参数（国密单向认证）
4. 初始化分段下载任务（3个并发，5MB分段）
5. 模拟中断（下载2个分段后中断）
6. 记录已下载分段信息
7. 重新初始化断点续传任务
8. 继续下载剩余分段
9. 合并分段文件
10. 验证文件MD5值与源对象一致

### 预期结果
- 上传任务成功完成，数据完整性校验通过
- 下载任务成功完成，数据完整性校验通过
- 国密SSL握手成功，使用国密算法套件

---

## 场景三：国密双向认证（双证书）

### 测试目的
验证断点续传功能在国密双向认证场景下的正确性，特别是国密特有的双证书模式（签名证书+加密证书）。

### 前置条件
1. OBS服务端已配置国密SSL双向认证
2. 有效的国密CA证书
3. 有效的国密客户端签名证书和私钥
4. 有效的国密客户端加密证书和私钥（国密特有）

### SSL配置
```c
// 启用国密模式
gm_mode_switch = OBS_GM_MODE_OPEN;

// 启用双向认证
mutual_ssl_switch = OBS_MUTUAL_SSL_OPEN;

// 服务端CA
server_cert_path = "/path/to/sm2_ca.pem";

// 签名证书（身份认证）
client_cert_path = "/path/to/sm2_sign_cert.pem";
client_key_path = "/path/to/sm2_sign_key.pem";

// 加密证书（国密密钥交换）- 国密特有双证书
client_enc_cert_path = "/path/to/sm2_enc_cert.pem";
client_enc_key_path = "/path/to/sm2_enc_key.pem";
```

### 测试步骤

#### 上传测试
1. 准备50MB测试文件，计算MD5值
2. 初始化obs_options结构体
3. 配置SSL参数（国密双向认证，含双证书）
4. 初始化分段上传任务
5. 模拟中断（上传2个分段后中断）
6. 记录已上传分段信息
7. 重新初始化断点续传任务
8. 继续上传剩余分段
9. 完成分段上传，合并对象
10. 验证对象MD5值与源文件一致

#### 下载测试
1. 获取已上传对象的元数据
2. 初始化obs_options结构体
3. 配置SSL参数（国密双向认证，含双证书）
4. 初始化分段下载任务（3个并发，5MB分段）
5. 模拟中断（下载2个分段后中断）
6. 记录已下载分段信息
7. 重新初始化断点续传任务
8. 继续下载剩余分段
9. 合并分段文件
10. 验证文件MD5值与源对象一致

### 预期结果
- 上传任务成功完成，数据完整性校验通过
- 下载任务成功完成，数据完整性校验通过
- 国密双向SSL握手成功
- 签名证书用于身份认证，加密证书用于密钥交换

---

## 场景四：国密密码套件配置

### 测试目的
验证断点续传功能在不同国密密码套件配置下的正确性。

### 前置条件
1. OBS服务端已配置国密SSL
2. 有效的国密CA证书
3. 服务端支持多种国密密码套件

### SSL配置
```c
// 启用国密模式
gm_mode_switch = OBS_GM_MODE_OPEN;

// 国密CA证书
server_cert_path = "/path/to/sm2_ca.pem";

// 国密密码套件（自定义配置）
ssl_cipher_list = "ECDHE-SM2-WITH-SM4-SM3:ECDHE-SM2-WITH-SM4-GCM-SM3";
```

### 测试步骤

#### 套件一：ECDHE-SM2-WITH-SM4-SM3
1. 准备50MB测试文件，计算MD5值
2. 初始化obs_options结构体
3. 配置SSL参数（国密模式，指定密码套件为ECDHE-SM2-WITH-SM4-SM3）
4. 执行断点续传上传和下载测试
5. 验证数据完整性

#### 套件二：ECDHE-SM2-WITH-SM4-GCM-SM3
1. 准备50MB测试文件，计算MD5值
2. 初始化obs_options结构体
3. 配置SSL参数（国密模式，指定密码套件为ECDHE-SM2-WITH-SM4-GCM-SM3）
4. 执行断点续传上传和下载测试
5. 验证数据完整性

#### 套件三：自动协商（默认配置）
1. 准备50MB测试文件，计算MD5值
2. 初始化obs_options结构体
3. 配置SSL参数（国密模式，不指定密码套件，使用默认值）
4. 执行断点续传上传和下载测试
5. 验证数据完整性

### 预期结果
- 所有密码套件配置下，上传任务成功完成，数据完整性校验通过
- 所有密码套件配置下，下载任务成功完成，数据完整性校验通过
- 国密SSL握手成功，协商出预期的密码套件

---

## 附录

### A. 测试环境准备

#### 服务端配置
1. 部署支持国密SSL的OBS服务端
2. 配置标准TLS和国密TLS监听端口
3. 配置双向认证所需的客户端CA证书
4. 准备测试用的桶（Bucket）

#### 客户端配置
1. 安装支持国密的libcurl库（基于Tongsuo）
2. 准备测试证书文件
3. 配置环境变量（Access Key, Secret Key, Endpoint等）

### B. 证书文件清单

| 证书类型 | 文件路径 | 用途 |
|---------|---------|-----|
| 标准CA证书 | /path/to/ca.pem | 服务端根证书验证 |
| 标准客户端证书 | /path/to/client_cert.pem | 客户端身份认证 |
| 标准客户端私钥 | /path/to/client_key.pem | TLS握手密钥交换 |
| 国密CA证书 | /path/to/sm2_ca.pem | 国密服务端根证书验证 |
| 国密签名证书 | /path/to/sm2_sign_cert.pem | 国密客户端身份认证（签名） |
| 国密签名私钥 | /path/to/sm2_sign_key.pem | 国密签名私钥 |
| 国密加密证书 | /path/to/sm2_enc_cert.pem | 国密客户端密钥交换（加密） |
| 国密加密私钥 | /path/to/sm2_enc_key.pem | 国密加密私钥 |

### C. 环境变量配置

```bash
export OBS_ACCESS_KEY_ID="your-access-key-id"
export OBS_SECRET_ACCESS_KEY="your-secret-access-key"
export OBS_ENDPOINT="https://your-obs-endpoint"
export OBS_BUCKET="your-test-bucket"
export OBS_REGION="your-region"

# 测试文件路径
export TEST_FILE_PATH="/path/to/test/file"
```

### D. 常见问题排查

#### Q1: 国密SSL握手失败
**可能原因**:
- libcurl未正确链接Tongsuo库
- 服务端未正确配置国密支持
- 证书格式不正确（SM2证书与普通证书格式不同）

**排查方法**:
1. 检查libcurl版本和编译参数
2. 使用openssl或tongsuo命令行工具验证证书
3. 抓包分析SSL握手过程

#### Q2: 双向认证失败
**可能原因**:
- 客户端证书与服务端信任的CA不匹配
- 证书过期或尚未生效
- 私钥密码错误（如果私钥加密）

**排查方法**:
1. 验证证书链完整性
2. 检查证书有效期
3. 使用openssl验证私钥密码

#### Q3: 断点续传中断后无法恢复
**可能原因**:
- 断点信息未正确保存
- 对象在断点期间被修改
- 权限变化导致无法访问

**排查方法**:
1. 检查断点信息记录
2. 验证对象ETag是否变化
3. 检查访问权限

---

## 文档变更历史

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|-----|
| v1.0 | 2024-01-15 | 初始版本 | SDK Team |
| v2.0 | 2024-03-20 | 修改字段名：client_sign_cert_path → client_cert_path；添加GM模式开关；添加国密双证书配置；拆分上传下载测试步骤 | SDK Team |
